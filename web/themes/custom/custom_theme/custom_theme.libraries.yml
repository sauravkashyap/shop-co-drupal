global-styling:
  css:
    theme:
      css/tokens.css: {}
      css/style.css: {}

# SDC components automatically include their CSS/JS files.
# No need to define them here unless they need special dependencies.
