{#
/**
 * @file
 * Theme override for a Layout Builder block containing the UI Builder JSON field.
 *
 * This strips out the Layout Builder block wrappers, rendering only the content.
 */
#}
{{ content }}
